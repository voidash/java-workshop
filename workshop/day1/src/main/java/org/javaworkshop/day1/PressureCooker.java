package org.javaworkshop.day1;

public class PressureCooker {
    // expected minutes is 10 minutes
    public int expectedMinutesToCookRice() {
        return 0;
    }

    // you pass the spent minutes. it should first check if spentMinutes is greater than the required minutes, in that case return -1
    // other wise you subtract expected minutes(10 min) to spent minutes
    // example : if 2 minutes has already passed then the remaining time should be 8
    public int remainingMinutesToCookRice(int spentMinutes) {
        return 0;
    }

    // should print "starting to cook rice"
    // case sensitive
    public String cookRice() {
        return "not passing";
    }
}
